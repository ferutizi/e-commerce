import './Nav.scss'

const Nav = () => {
    return(
        <nav className="nav--container">
            <h1 className="logo">Shop</h1>
            <button className="btn--carro">
                carro
            </button>
        </nav>
    )
}

export default Nav